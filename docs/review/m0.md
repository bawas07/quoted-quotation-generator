All findings validated. Now here is the consolidated review report:
Task Review: Working Tree (src/) → HEAD (f15e101)
Acceptance criteria coverage: Partial — Layout shell renders ✅, types defined ✅, tokens match ✅, localStorage tested ✅. But composable list diverges from spec (7 missing), and component architecture has inline-vs-stubs fork.
Reviewers: 2 — code-reviewer + architect-reviewer (structural change across ~50 files; security surface is negligible for client-side-only M0 scaffolding)
Diff: 4 modified + 46 new files under src/, 429 additions / 394 deletions in tracked diff
Must-fix
1. App.vue has all layout rendered inline — 22 component stubs are dead code — src/App.vue (433 lines)
Problem: App.vue renders the entire two-column layout (sidebar + preview) inline with 336 lines of scoped CSS, replicating classes from example-v2.html. All 22 component stubs in components/ are never imported or used. This means the button styles (.btn, .btn-primary, etc.) are scoped to App.vue and won't apply to AppButton.vue when implemented. When M1 begins, developers must either delete the inline code and rewire everything to stubs, or abandon the stubs — either path wastes M0 effort.
Fix: Refactor App.vue to compose <SidebarShell>, <PreviewShell>, etc. Move sidebar-specific CSS to SidebarShell.vue, preview CSS to PreviewShell.vue, button CSS to AppButton.vue. Keep only the grid layout (.app) in App.vue.
Raised by: architect-reviewer (CRITICAL), echoed by code-reviewer (MEDIUM)
2. Composable list diverges from milestone spec — 7 stubs missing — src/composables/ directory
Problem: The M0 spec lists 11 required composables. Only 5 exist (useQuotation, useHistory, useCatalog, useTemplates, useLocalStorage). Six are entirely missing: useCatalogSync.ts, useAutocomplete.ts, useFuzzyMatch.ts, useLogoUpload.ts, useJsonIO.ts, useWorkspaceIO.ts. One has a naming mismatch (useTemplate.ts vs useTemplates.ts). Additionally, useLocalStorage.ts is redundant (see should-fix #5). This means the "Stub all composables" M0 criterion is only ~45% met for the specified list.
Fix: Add the 7 missing stubs. Rename useTemplates.ts → useTemplate.ts. Remove useLocalStorage.ts.
Raised by: architect-reviewer (CRITICAL), code-reviewer (HIGH)
3. fuzzyMatch.ts returns boolean — M2 spec requires similarity score 0–1 — src/utils/fuzzyMatch.ts:3
Problem: The function signature is fuzzyMatch(query, target): boolean. The M2 milestone spec (line 170–174) defines it as fuzzyMatch(query, target): number returning a 0–1 similarity score. Also missing the fuzzySearch(query, catalog) companion function (line 175). The reference example-v2.html lines 919–931 implement word-based scoring. M2's autocomplete (top 5 ranked matches) and M3's catalog sync (≥0.85 threshold for UPDATE classification) both depend on scored results — a boolean return is structurally incompatible with both features.
Fix: Change return type to number (0–1), implement bigram/Jaccard similarity matching the reference, and add fuzzySearch() export.
Raised by: code-reviewer (CRITICAL), architect-reviewer (HIGH)
4. formatCurrency.ts defaults to PHP/en-PH — should default to IDR — src/utils/formatCurrency.ts:3
Problem: formatCurrency(amount: number, currency: string = 'PHP') uses 'en-PH' locale. The reference design (example-v2.html:544) selects IDR by default. defaults.ts correctly has DEFAULT_CURRENCY = 'IDR'. Any call omitting the currency parameter silently formats as Philippine peso (₱1,000,000.00) instead of Indonesian rupiah (Rp1.000.000). The example-v2.html also handles IDR/JPY with no decimals, which is missing here.
Fix: Change default to 'IDR', locale to 'id-ID', add zero-decimal handling for IDR and JPY currencies per the reference implementation.
Raised by: code-reviewer (CRITICAL), architect-reviewer (MEDIUM)
5. calculations.ts uses rate naming — doesn't align with LineItem.unit_price type — src/utils/calculations.ts:3–8
Problem: calculateLineItemTotal(quantity, rate) and calculateSubtotal(items: Array<{ quantity, rate }>) use the parameter name rate. The LineItem interface uses unit_price. This means calculateSubtotal cannot accept a LineItem[] directly — every consumer must map { quantity: i.quantity, rate: i.unit_price }. The M1 milestone specifies calcSubtotal(), calcDiscount(), calcTax(), calcTotal() — different names and missing calcDiscount() entirely.
Fix: Rename rate → unitPrice (camelCase convention for parameters). Accept Pick<LineItem, 'quantity' | 'unit_price'>. Add calculateDiscount(). Rename functions to calcSubtotal, calcTax, calcTotal per M1 spec.
Raised by: code-reviewer (HIGH), architect-reviewer (HIGH)
6. generateFilename.ts format doesn't match M1 spec QUO-001_ClientName_2026-05-19.json — src/utils/generateFilename.ts:5–11
Problem: The M1 spec (line 130) defines the format as QUO-001_ClientName_2026-05-19.json — underscore separators, [number]_[client]_[date] order. Current implementation uses hyphens, reversed order [prefix]-[date]-[number], and has no client name parameter. The reference example-v2.html:1056 matches the spec format exactly.
Fix: Change signature to generateFilename(quotationNumber, clientName, date): string and produce {number}_{client}_{date}.json format per spec.
Raised by: code-reviewer (HIGH), architect-reviewer (MEDIUM)
Should-fix
1. index.html title is "quotation-generator" — should match brand — index.html:10
The reference example-v2.html:6 uses Quoted - A Quotation Generator. The current title is the kebab-case project name. This shows in the browser tab.
→ Change <title> to match the design.
Raised by: code-reviewer
2. Preview component naming diverges from milestone spec — src/components/preview/
Spec expects PreviewPanel.vue, TemplateSwitcher.vue, StatusBar.vue but code has PreviewShell.vue, PreviewToolbar.vue, PreviewEmpty.vue. M4 tasks reference components by spec names — developers won't find them. Rename to match spec (PreviewPanel, TemplateSwitcher, StatusBar) while keeping PreviewEmpty as a sub-component.
Raised by: architect-reviewer (MEDIUM), echoed by code-reviewer
3. Template components missing "Template" prefix — src/components/preview/templates/
Spec lists TemplateClassic.vue, TemplateMinimal.vue, etc. but files are Classic.vue, Minimal.vue, etc. The prefix provides self-documentation — Classic.vue is ambiguous, TemplateClassic.vue is clear.
→ Rename to include Template prefix per spec.
Raised by: code-reviewer
4. localStorage.test.ts has stale "quotify_" comments — src/utils/localStorage.test.ts:7,107,134,142
Four test descriptions/comments reference quotify_ prefix but the actual implementation and test constant use quoted_. Misleading for readers.
→ Replace all quotify_ references with quoted_ in test comments.
Raised by: code-reviewer, architect-reviewer
5. useLocalStorage.ts composable is redundant with utils/localStorage.ts — src/composables/useLocalStorage.ts
utils/localStorage.ts already provides a complete typed storage object. The empty composable stub adds a layer of indirection with no added value. It's not in the milestone spec either.
→ Remove the file. Consumers should import directly from utils/localStorage.ts.
Raised by: code-reviewer, architect-reviewer
6. estimateRemaining() in localStorage.ts has scoping issue and unnecessary conditional — src/utils/localStorage.ts:84–101
let size is block-scoped inside try but the catch block comment says "size now holds the max" — it's inaccessible there. The if (testKey) guard on line 97 is always truthy (testKey is a constant string). The removeItem call at line 98 isn't wrapped in try-catch.
→ Move size outside try, remove unnecessary if, wrap cleanup in try-catch.
Raised by: code-reviewer
7. No src/utils/index.ts barrel export — src/utils/
Utilities are individually importable but there's no barrel module. Beneficial to establish now before the util surface grows through M1–M6.
→ Add barrel re-exports.
Raised by: architect-reviewer
8. calculations.ts calculateGrandTotal subtracts discount before or after tax? — src/utils/calculations.ts:15–17
The function does subtotal + tax - discount (flat subtraction). The example-v2.html:937–939 applies discount to subtotal first, then tax on the discounted amount: base = subtotal - discount; tax = base * tax%; total = base + tax. This semantic difference matters for tax compliance (PPN at 11% of the discounted amount).
→ Verify the intended business logic and align with the reference implementation.
Raised by: architect-reviewer
Validation note: 18 findings identified, 5 overlapping across reviewers → 14 consolidated (6 must-fix, 8 should-fix). Dropped 0 findings as unverifiable. All cited lines confirmed via grep/file reads.
